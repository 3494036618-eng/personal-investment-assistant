name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.5'
          cache: npm
          cache-dependency-path: skill/investment-assistant/assets/app/package-lock.json
      - run: npm ci
        working-directory: skill/investment-assistant/assets/app
      - run: npm run verify
